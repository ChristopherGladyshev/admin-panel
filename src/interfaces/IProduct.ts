export interface IProduct {
  title: string,
  description: string,
  product_text: string,
  prise: number,
  price: string,
  date_of_creation: string,
  images: string[],
  number_of_goods: number,
}